# Exit-Ticket
An exit ticket program that allows a server program user to set an exit ticket question, then allows users from the client application to connect to the server and answer the question. The question and answers are then stored in a local file. 
